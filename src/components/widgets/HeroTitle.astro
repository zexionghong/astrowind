---
import { twMerge } from 'tailwind-merge';
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'section'> {
  title: string;
  subtitle: string;
  buttonText: string;
  buttonHref: string;
  classes?: {
    container?: string;
    title?: string;
    subtitle?: string;
    button?: string;
  };
  bg?: string;
  id?: string;
}

const {
  title,
  subtitle,
  buttonText,
  buttonHref,
  classes = {},
  bg = '',
  id,
} = Astro.props;

const {
  container: containerClass = '',
  title: titleClass = '',
  subtitle: subtitleClass = '',
  button: buttonClass = '',
} = classes;
---

<section class="relative min-h-[200px]" {...id ? { id } : {}}>
  {
    bg && (
      <Fragment set:html={bg} />
    )
  }
  <div class={twMerge('relative z-10 mx-auto max-w-xl px-4 md:px-6 pt-12 md:pt-16 lg:pt-10 text-default text-center', containerClass)}>
    <div class="glass-card rounded-t-3xl p-6 md:p-8 lg:p-10 relative overflow-hidden">
      <div class="relative z-10">
        <h2 class={twMerge('text-4xl md:text-4xl lg:text-4xl font-bold text-white mb-6', titleClass)}>
          {title}
        </h2>
        <p class={twMerge('text-sm md:text-sm text-white mb-8 whitespace-pre-line', subtitleClass)}>
          {subtitle}
        </p>
        <a
          href={buttonHref}
          class={twMerge('inline-block bg-primary hover:bg-blue-700 text-white font-semibold py-4 px-10 rounded-full transition-all duration-300 hover:shadow-glass-lg hover:scale-105', buttonClass)}
        >
          {buttonText}
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .glass-card {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    box-shadow: 0 8px 32px rgba(31, 38, 135, 0.1);
    isolation: isolate;
  }

  .glass-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(
      45deg,
      rgba(255, 255, 255, 0.1) 0%,
      rgba(255, 255, 255, 0.2) 100%
    );
    border-radius: inherit;
    z-index: -1;
  }

  .glass-card > * {
    isolation: isolate;
  }
</style> 