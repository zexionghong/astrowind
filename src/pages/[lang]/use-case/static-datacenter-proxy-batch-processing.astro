---
import Layout from '~/layouts/PageLayout.astro';
import { useTranslations } from '~/utils/i18n';
import WhyChooseUs from '~/components/widgets/WhyChooseUs.astro';
import FAQs from '~/components/widgets/FAQs.astro';
import heroImage from '~/assets/images/proxies/3-1.webp';

export async function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'zh' } }
  ];
}

const { lang } = Astro.params;
const t = useTranslations(lang);
const cases = t('datacenterMoreCases');

---

<Layout
  metadata={{
    title: t('batchProcessing.metadata.title'),
    description: t('batchProcessing.metadata.description'),
    ignoreTitleTemplate: true,
  }}
>
  <!-- Hero Section -->
  <section class="relative bg-gradient-to-br from-blue-900 via-indigo-900 to-purple-900 text-white overflow-hidden">
    <div class="absolute inset-0 bg-black/20"></div>
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-24">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <div>
          <h1 class="text-4xl md:text-5xl font-bold mb-6 leading-tight">
            {t('batchProcessing.hero.title')}
          </h1>
          <p class="text-xl mb-8 text-blue-100">
            {t('batchProcessing.hero.subtitle')}
          </p>
          <ul class="space-y-4 mb-8">
            {(t('batchProcessing.hero.features') || []).map((feature: string) => (
              <li class="flex items-center gap-3">
                <svg class="w-6 h-6 text-green-400 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span class="text-lg">{feature}</span>
              </li>
            ))}
          </ul>
          <div class="flex items-center gap-4 pt-4">
             <a  href="https://dashboard.ipflex.ink/register" class="bg-[#00E062] hover:bg-[#00C054] text-[#1A1147] font-semibold px-6 py-3 rounded-lg transition-all duration-200">
              {t('batchProcessing.hero.cta.primary')}
            </a>
          </div>
        </div>
        <div class="relative">
          <img src={heroImage.src} alt="Static Datacenter Proxy Batch Processing" class="rounded-2xl shadow-2xl" />
        </div>
      </div>
    </div>
  </section>

  <section class="max-w-6xl mx-auto py-10">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <!-- 场景卡片 -->
      <div class="rounded-xl shadow-lg border p-7 bg-gradient-to-br from-blue-50 to-white">
        <h2 class="text-3xl font-bold mb-4 text-[#1E40AF] flex items-center gap-2">
          <svg width="30" height="30" fill="none" stroke="#3B82F6" stroke-width="3"><circle cx="15" cy="15" r="14" /></svg>
          {t('batchProcessing.scenario.title')}
        </h2>
        <ul class="divide-y divide-[#93C5FD]">
          {(t('batchProcessing.scenario.steps') || []).map((step: string, idx: number) => (
            <li class="py-4 flex items-center gap-3">
              <span class="inline-flex items-center justify-center w-10 h-10 text-lg bg-[#3B82F633] rounded-full font-bold text-[#1E40AF]">{idx + 1}</span>
              {step}
            </li>
          ))}
        </ul>
      </div>

      <!-- 优势/痛点卡片 -->
      <div class="rounded-xl shadow-lg border p-7 bg-gradient-to-br from-slate-50 to-white">
        <h2 class="text-2xl font-bold mb-4 text-[#ba732c] flex items-center gap-2">
          <svg width="28" height="28" fill="none" stroke="#F59E42" stroke-width="2.5"><rect x="4" y="4" width="20" height="20" rx="10" /></svg>
          {t('batchProcessing.painGain.title')}
        </h2>
        <div class="flex flex-col gap-2">
          {(t('batchProcessing.painGain.pros') || []).map((pro: string) => (
            <div class="bg-green-100/80 px-3 py-2 rounded flex gap-2 items-center">
              <span class="font-bold text-[#14a67d]">✓</span> {pro}
            </div>
          ))}
          {(t('batchProcessing.painGain.cons') || []).map((con: string) => (
            <div class="bg-red-100/80 px-3 py-2 rounded flex gap-2 items-center">
              <span class="font-bold text-[#dc2626]">✗</span> {con}
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- 工作流程 -->
  <section class="max-w-6xl mx-auto py-10">
    <h2 class="text-3xl font-bold text-center mb-8">{t('batchProcessing.flow.title')}</h2>
    <div class="grid grid-cols-1 md:grid-cols-5 gap-6">
      {(t('batchProcessing.flow.steps') || []).map((step: string, idx: number) => (
        <div class="text-center">
          <div class="w-16 h-16 bg-[#3B82F6] text-white rounded-full flex items-center justify-center text-xl font-bold mx-auto mb-4">
            {idx + 1}
          </div>
          <p class="text-sm font-medium">{step}</p>
        </div>
      ))}
    </div>
  </section>

  <WhyChooseUs
    title={t('batchProcessing.whyChooseUs.title')}
    features={t('batchProcessing.whyChooseUs.features')}
    classes={{
      container: 'max-w-6xl',
      title: 'text-3xl font-bold text-center mb-12',
    }}
  />

  <section class="max-w-5xl mx-auto py-10">
    <h2 class="text-2xl font-semibold mb-4">{t('datacenterMoreCasesTitle') || '更多静态数据中心代理应用场景'}</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-6">
      {cases.map((x) => (
        <a href={x.url} class="block p-4 rounded-lg border-2 border-[#3B82F6] bg-blue-50 text-[#1E40AF] font-semibold cursor-pointer">
          {x.title}
          <div class="text-xs text-gray-500 mt-2">{x.description}</div>
        </a>
      ))}
    </div>
  </section>

  <section class="max-w-5xl mx-auto py-8 px-4">
    <h2 class="text-2xl font-bold mb-4 text-gray-900 dark:text-white">
      {lang === 'zh' ? '批处理任务的并发与重试模型' : 'Concurrency and Retry Model for Batch Processing'}
    </h2>
    <p class="text-base leading-7 text-gray-700 dark:text-gray-300">
      {lang === 'zh'
        ? '批处理场景建议采用分批次并行而不是一次性全量并发。静态数据中心代理在批量任务中可以提供更稳定的出口一致性，适合长队列处理。为避免失败放大，建议设置批次级别熔断阈值，并为超时、限流、解析失败建立分级重试策略。这样既能保证任务完成率，也能有效控制单位数据处理成本。'
        : 'For batch workloads, prefer staged parallel execution over all-at-once bursts. Static datacenter proxies provide consistent exits for long queue processing. To prevent failure amplification, set batch-level circuit breakers and tiered retries for timeout, rate-limit, and parsing errors. This approach improves completion rate while controlling cost per processed record.'}
    </p>
  </section>

  <FAQs
    title={t('batchProcessing.faqs.title')}
    subtitle={t('batchProcessing.faqs.subtitle')}
    tagline={t('batchProcessing.faqs.tagline')}
    items={t('batchProcessing.faqs.items')}
  />
</Layout>
